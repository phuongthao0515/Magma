if (!window.config) {
  window.config = {};
}

window.config.baseApiUrl = window.config.baseApiUrl || "http://localhost:8000";
window.config.agentApiUrl = window.config.agentApiUrl || "http://localhost:8010";
